---
# The home page carries its own `portrait` so the profile header has an avatar
# without filling the About page's frame — that one is deliberately left empty
# to show the fallback. Page front matter beats params.portrait, which is the
# same override any page gets.
#
# It points at the favicon: this demo site is a theme, not a person, so its
# wordmark is the honest avatar. Swap it for a photo on a real site.
portrait: "favicon.png"
---
